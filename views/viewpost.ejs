<!DOCTYPE html>
<html lang="de">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Artikel-Container mit Bootstrap</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #2c2c2c; /* Dunkler Hintergrund */
            color: #dcdcdc; /* Helle Schriftfarbe */
            font-family: 'Times New Roman', Times, serif; /* Serifen-Schriftart */
            font-size: 16px;
            padding: 20px;
        }

        .article-container {
            padding: 20px;
            background-color: #333333; /* Dunkles Grau */
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            margin-bottom: 20px;
        }

        .article-image {
            max-width: 150px;
            height: auto;
            border-radius: 10px;
            margin-right: 20px;
        }

        .article-title {
            font-size: 2rem; /* Größere Überschrift */
            font-weight: bold;
        }

        .article-name, .article-date {
            font-size: 1rem; /* Kleinere Schrift für Datum und Benutzername */
            color: #a0a0a0; /* Hellgraue Farbe */
        }

        .notebook-container {
            position: relative;
            background-color: #4c4848; /* Dunkles Braun */
            border: 1px solid #464141;
            border-radius: 20px;
            padding: 15px;
            margin-top: 15px;
            box-shadow: inset 0 0 0 1px #100707;
            overflow: hidden;
        }

        .notebook-container:before {
            content: "";
            position: absolute;
            top: 0;
            bottom: 0;
            left: 0;
            width: 100%;
            background: rgb(60, 61, 62);
            z-index: 1;
            border-radius: 20px;
            pointer-events: none;
        }

        .notebook-container:after {
            content: "";
            position: absolute;
            top: 0;
            bottom: 0;
            left: 50px;
            width: 1.5px;
            background-color: #080808;
            z-index: 1;
            pointer-events: none;
        }

        .article-text {
            position: relative;
            z-index: 2;
            padding-left: 60px;
            padding-right: 20px; /* Zusätzlicher rechter Padding, um Platz zu schaffen */
            font-size: 18px; /* Kleinere Schrift für den Artikeltext */
            line-height: 1.8;
            text-align: justify; /* Blocksatz für einen ordentlichen Textfluss */
            white-space: pre-line; /* Zeilenumbrüche im Text beibehalten */
            word-wrap: break-word; /* Lange Wörter umbrechen */
            overflow-wrap: break-word; /* An Wortgrenzen umschlagen */
        }

        .article-header {
            display: flex;
            align-items: center;
        }
    </style>
</head>

<body>
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <div class="article-container">
                    <div class="article-header">
                        <img src="<%= image %>" alt="Artikel Bild" class="article-image">
                        <div>
                            <div class="article-title"><%= title %></div>
                            <div class="article-date"><%= datum %></div>
                            <div class="article-name"><%= benutzername %></div>
                        </div>
                    </div>
                    <div class="notebook-container">
                        <div class="article-text">
                            <%= text %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Optional Bootstrap JS (if needed for Bootstrap JS functionality) -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/js/bootstrap.min.js"></script>
</body>

</html>
